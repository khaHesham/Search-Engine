<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <!--  This file has been downloaded from bootdey.com @bootdey on twitter -->
    <!--  All snippets are MIT license http://bootdey.com/license -->
    <title>Search results</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
	<script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
<div class="content">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="search-result-box card-box">
                    <div class="row">
                        <div class="col-md-8 offset-md-2">
                            <div class="pt-3 pb-4">
                                <form action="" method="post" id="search-form">
                                    {{search_form.csrf_token}}

                                    <div class="input-group">
                                            {{search_form.Key(id="search", placeholder="Type Keywords", class="form-control")}}
                                            <div class="input-group-append">
                                                {{search_form.searchBtn(id="searchKey", class="btn waves-effect waves-light btn-custom")}}
                                            </div>
                                    </div>
                                </form>
                               

                                <div class="mt-4 text-center">
                                    <h4>Search Results For "{{ searchPhrase }}"</h4></div>
                            </div>
                        </div>
                    </div>
                    <!-- end row -->
                    <ul class="nav nav-tabs tabs-bordered">
                        <li class="nav-item"><a href="#home" data-toggle="tab" aria-expanded="true" class="nav-link active">All results <span class="badge badge-success ml-1">{{ numberOfResults }}</span></a></li>
                    </ul>
                    <div class="tab-content">
                        <div class="tab-pane active" id="home">
                            <div class="row">
                                <div class="col-md-12">
                                    
                                    {% for result in result_list %}
                                        {% set link = "//" + result['link'] %}
                                        <div class="search-item">
                                            <h4 class="mb-1"><a href={{link}}>{{result['title']}}</a></h4>
                                            <div class="font-13 text-success mb-3">
                                                <a href = {{link}} >
                                                   {{result['link']}}
                                                </a>                                            
                                            </div>
                                            <p class="mb-0 text-muted">{{result['description']}}</p>
                                        </div>
                                    
                                    {% endfor %}

                                    <ul class="pagination justify-content-end pagination-split mt-0">
                                    {% for i in range(numberOfPages) %}
                                        {% set link = "/search?q=" + searchPhrase + "&page=" + (i+1)|string %}
                                        {% if (i + 1)|int == page|int %}
                                            <li class="page-item active"><a class="page-link" href={{link}}>{{ i + 1 }}</a></li>
                                        {% else %}
                                            <li class="page-item"><a class="page-link" href={{link}}>{{ i + 1 }}</a></li>
                                        {% endif %}
                                    {% endfor %}
                                    </ul>
                                    <div class="clearfix"></div>
                                </div>
                            </div>
                        </div>
                        <!-- end All results tab -->
                    </div>
                </div>
            </div>
        </div>
        <!-- end row -->
    </div>
    <!-- container -->
</div>

<style type="text/css">
body{
    margin-top:20px;
    background:#dcdcdc;
}
.card-box {
    padding: 20px;
    border-radius: 3px;
    margin-bottom: 30px;
    background-color: #fff;
}
.search-result-box .tab-content {
    padding: 30px 30px 10px 30px;
    -webkit-box-shadow: none;
    box-shadow: none;
    -moz-box-shadow: none
}

.search-result-box .search-item {
    padding-bottom: 20px;
    border-bottom: 1px solid #e3eaef;
    margin-bottom: 20px
}
.text-success {
    color: #0acf97!important;
}
a {
    color: #007bff;
    text-decoration: none;
    background-color: transparent;
}
.btn-custom {
    background-color: #02c0ce;
    border-color: #02c0ce;
}

.btn-custom, .btn-danger, .btn-info, .btn-inverse, .btn-pink, .btn-primary, .btn-purple, .btn-success, .btn-warning {
    color: #fff!important;
}
</style>

<script type="text/javascript">

</script>
</body>
</html>